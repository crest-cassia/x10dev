#ifndef MAIN_KUMPULA
#define MAIN_KUMPULA

int RunSimulator(
    int net_size,
    double p_tri,
    double p_jump,
    double delta,
    double p_nd,
    double p_ld,
    double aging,
    double w_th,
    double alpha,
    double mobility,
    double noise,
    double p_flight,
    double beta,
    int t_max,
    int seed
  );

#endif // MAIN_KUMPULA

